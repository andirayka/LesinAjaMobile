export type PilihanLesType = {
  nama: string;
  harga: string;
};

export type StudentType = {
  idortu: number;
  idsiswa: number;
  jeniskelamin: 'Laki-laki' | 'Perempuan';
  jenjang: string;
  kelas: string;
  siswa: string;
};

export type DaerahType = {
  name: string;
  id: string;
};
